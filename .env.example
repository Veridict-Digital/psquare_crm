# Copy this file to .env and fill in real values before deploying
# Never commit .env to git

# ── Django ────────────────────────────────────────────────────────────────────
DJANGO_SECRET_KEY=replace-this-with-a-long-random-string
DEBUG=False
# Comma-separated: your Coolify domain + localhost for health checks
ALLOWED_HOSTS=psquareherbocos.com,localhost
# Comma-separated origins the browser is allowed to call the API from
CORS_ALLOWED_ORIGINS=https://psquareherbocos.com

# ── Database ──────────────────────────────────────────────────────────────────
# Copy the DATABASE_URL from your Coolify PostgreSQL resource's connection details
DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Email ─────────────────────────────────────────────────────────────────────
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=yourmail@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password
DEFAULT_FROM_EMAIL=yourmail@gmail.com
